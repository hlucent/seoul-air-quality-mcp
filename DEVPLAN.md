# DEVPLAN.md — 서울시 대기환경정보 MCP 확장 (7개 데이터셋 추가)

## 0. 배경 및 목적

기존 `seoul-air-quality-mcp`(서울시 대기환경정보 MCP)에 신규 데이터셋을 추가
통합한다. 기존 MCP는 대기정책과 10개, 기타(위치·시설) 3개, 보건환경연구원 3개,
총 16개 도구를 이미 보유하고 있으며, 이번 확장은 그와 **중복되지 않는 데이터셋**을
추가하는 작업이다.

### 0-1. 작업 중 발견된 기존 버그 (중요 — 반드시 함께 처리)

기존 저장소 `main.py`를 실측 대조하는 과정에서, **기존 도구
`get_realtime_air_quality`(OA-1200 "서울시 실시간 자치구별 대기환경 현황"으로
문서화되어 있음)가 실제로는 잘못된 API를 호출하고 있는 것을 확인했다.**

- README.md 및 `main.py` 528행에서 실제로 호출하는 서비스명: `RealtimeCityAir`
- `RealtimeCityAir`의 진짜 데이터셋: **"서울시 권역별 실시간 대기환경 현황"**
  (도심권/동북권/동남권/서북권/서남권 단위)
- "서울시 실시간 자치구별 대기환경 현황"(25개 자치구 단위)의 진짜 서비스명은
  **`ListAirQualityByDistrictService`**이며, 이 서비스는 `main.py`에 구현된 적이 없음
  (`Select-String`으로 전체 코드 검색 결과 미검출 확인).

**원인 추정**: README.md 개발 일지 3단계(2026-08-01) 기록에 "확인된 서비스명:
`RealtimeCityAir` (OA-1200, 실시간 자치구별 대기환경)"이라고 되어 있어, 개발
초기 단계에서 두 데이터셋(자치구별 vs 권역별)의 서비스명을 착각해 잘못
연결한 것으로 보인다. 이후 문서화도 이 착오를 그대로 답습해 최종 표까지
`RealtimeCityAir` = 자치구별로 기재되어 왔다.

**처리 방침 (사용자 확정)**: 아래 두 가지를 모두 이번 확장 작업에 포함한다.
1. 기존 `get_realtime_air_quality` 도구를 `get_zonal_realtime_air_quality`로
   개명 — 실제 호출 대상(`RealtimeCityAir`, 권역별)에 맞는 이름·설명·docstring으로
   정정. 내부 로직(API 호출 자체)은 변경하지 않는다 — 이미 정상적으로
   `RealtimeCityAir`를 호출하고 있으므로 이름과 문서만 실제 동작에 맞춘다.
2. `get_realtime_air_quality`라는 이름은 이번에 **진짜** "서울시 실시간
   자치구별 대기환경 현황"(`ListAirQualityByDistrictService`)에 신규로
   부여해 새로 구현한다.

이 정정으로, 원래 신규 추가 대상으로 검토했던 "서울시 권역별 실시간 대기환경
현황"(1-6절 참고)은 **완전 신규가 아니라 기존 도구의 개명·재문서화 대상**임이
확정되었다. 대신 진짜 "서울시 실시간 자치구별 대기환경 현황"이 신규 목록에
새로 들어간다.

### 0-2. 최종 확장 범위

- **개명 대상(1개)**: `get_realtime_air_quality` → `get_zonal_realtime_air_quality`
  (코드 로직 변경 없음, 이름·설명만 정정)
- **신규 추가(6개)**: 아래 3절 참고 — 서울시 실시간 자치구별 대기환경 현황(신규
  진짜 구현), 서울시 연도별 오존 경보발령 현황, 서울시 지역 구별 측정소
  행정코드 정보, 서울시 년도별 평균 대기오염도 정보, 서울시 초미세먼지 연도별
  발령정보, 서울시 굴뚝 측정 정보
- **표기 정정만(1개, 코드 변경 없음)**: `get_hourly_air_quality`의 화면 표시명
  "서울시 시간평균 대기오염도 정보" → "서울시 시간 평균 대기오염도 정보"
  (띄어쓰기 정정)

확장 후 전체 도구 수: 기존 16개 + 신규 6개 = **22개** (개명 대상은 이미 16개
안에 포함되어 있으므로 개수에 영향 없음)

---

## 1. API 스펙 요약 (7개 신규 데이터셋)

모든 데이터셋은 서울 열린데이터광장(data.seoul.go.kr) 제공, 공통 요청 구조:

```
http://openAPI.seoul.go.kr:8088/{KEY}/{TYPE}/{SERVICE}/{START_INDEX}/{END_INDEX}/{선택파라미터...}
```

공통 필수 파라미터: `KEY`(인증키), `TYPE`(xml/json 등), `SERVICE`(서비스명),
`START_INDEX`, `END_INDEX`
공통 에러코드: INFO-000(정상), ERROR-300(필수값 누락), INFO-100(인증키 오류),
ERROR-301(TYPE 오류), ERROR-310(SERVICE 오류), ERROR-331~336(위치값 오류),
ERROR-500/600/601(서버/DB 오류), INFO-200(데이터 없음)

### 1-1. 서울시 연도별 오존 경보발령 현황 (YearlyOzoneIssue)

- **제공부서**: 기후환경본부 대기정책과
- **설명**: 서울시 연도별 오존 경보발령 횟수, 일수, 최대농도 정보
- **선택 파라미터**: `YR`(년도)
- **응답 필드**: `YR`(년도), `APNT_NMTM`(발령횟수), `APNT_DCNT`(발령일수), `MAX_DNST`(최고농도, ppm)
- **실측 확인 필요**: 예제 응답(1995~1999년)에서 `MAX_DNST`가 빈 값(`<MAX_DNST/>`)으로
  옴 — 발령 횟수가 0인 연도는 최대농도 필드가 공백일 수 있음. 빈 문자열/None 처리 필요.

### 1-2. 서울시 지역 구별 측정소 행정코드 정보 (SearchMeasuringSTNOfAirQualityService)

- **제공부서**: 기후환경본부 대기정책과
- **설명**: 자치구별로 부여된 측정소 행정코드(MSRSTN_CD)와 측정소명을 조회. 다른
  도구(`get_realtime_air_quality`의 `MSRSTN_PBADMS_CD` 등)에서 쓰이는 코드값을
  역으로 찾을 때 유용한 조회용 도구.
- **선택 파라미터**: `MSRSTN_CD`(측정소코드), `MSRSTN_NM`(측정소명)
- **응답 필드**: `MSRSTN_CD`(측정소코드), `MSRSTN_NM`(측정소명)
- **비고**: 총 25개 자치구 코드 매핑표. 다른 서울시 대기환경 API의 구 코드
  (예: 111151=중랑구)와 일치하는지 실측 대조 권장.

### 1-3. 서울시 년도별 평균 대기오염도 정보 (YearlyAverageAirQuality)

- **제공부서**: 기후환경본부 대기정책과
- **필수 파라미터**: `MSRMT_YR`(측정년도, YYYY) — **주의: START_INDEX/END_INDEX
  외에 연도 자체가 필수 파라미터**
- **선택 파라미터**: `MSRSTN_NM`(측정소명)
- **응답 필드**: `MSRMT_YR`(측정년도), `MSRSTN_NM`(측정소명), `NTDX`(이산화질소, ppm),
  `OZON`(오존, ppm), `CBMX`(일산화탄소, ppm), `SPDX`(아황산가스, ppm), `PM`(미세먼지, ㎍/㎥),
  `FPM`(초미세먼지, ㎍/㎥)
- **비고**: 기존 16개 중 `get_monthly_air_quality`(월별), `get_daily_air_quality`(일별)와
  유사한 패턴이나 연 단위 집계라는 점에서 구분됨. 측정소명에는 일반 자치구명 외에
  도로변 측정소명(예: "강남대로", "강변북로")도 포함되는 것으로 예제에서 확인됨.

### 1-4. 서울시 초미세먼지 연도별 발령정보 (yearMicroDustInfo)

- **제공부서**: 기후환경본부 대기정책과
- **설명**: 초미세먼지 주의보예비단계/주의보/경보 발령 횟수·일수, 연도별 최대농도
- **선택 파라미터**: `YR`(년도)
- **응답 필드**: `YR`(년도), `RSRV_NMTM`(주의보예비단계발령횟수), `RSRV_DCNT`(주의보예비단계발령일수),
  `FRCST_NMTM`(주의보발령횟수), `FRCST_DCNT`(주의보발령일수), `WARN_NMTM`(경보발령횟수),
  `WARN_DCNT`(경보발령일수), `MAX_DNST`(최대농도)
- **실측 확인 필요**: 예제에서 발령 횟수가 0인 연도(2016)는 `MAX_DNST`가 빈 값
  (`<MAX_DNST/>`)으로 옴 — 1-1과 동일한 패턴. 빈 문자열/None 안전 처리 필요.
- **1-1(오존 경보)과의 구분**: 이 데이터셋은 초미세먼지 전용이며, 주의보예비단계까지
  3단계로 세분화되어 있다는 점이 오존 경보(2단계: 발령횟수/일수만)와 다름. 도구명과
  설명에 "초미세먼지"임을 명확히 표기해 오존 경보발령 도구와 혼동되지 않게 한다.

### 1-5. 서울시 굴뚝 측정 정보 (CleanSYSService)

- **제공부서**: **기후환경본부 자원회수시설추진단 자원회수시설과** (대기정책과 아님 — 별도 부서)
- **설명**: 4개 자원회수시설(강남, 노원, 마포, 양천)의 굴뚝 배출가스 자동측정 정보
- **선택 파라미터**: `FCLT_NM`(시설명), `ARTCL`(항목)
- **응답 필드**: `MSRMT_DT`(측정일시), `FCLT_NM`(시설명), `STACK_NM`(굴뚝명), `ARTCL`(항목,
  CDATA로 감싸져 있음 — 예: "먼지(Dust)"), `MSRMT_VL`(측정값)
- **실측 확인 필요**: 예제 응답에서 `MSRMT_VL`이 숫자가 아니라 **"운휴중"이라는 문자열**로
  올 수 있음 확인됨 — 시설 가동을 멈춘 시간대에는 측정값 필드가 숫자가 아닌 상태
  문자열로 대체되는 것으로 보임. 숫자 파싱 시 반드시 이 문자열 케이스를 먼저 걸러내는
  안전 변환 로직 필요 (2-2절 `_safe_float`/`_safe_int` 확장 검토 — "운휴중" 외에
  "점검중" 등 다른 상태 문자열이 있는지도 실측 단계에서 추가 확인).
- **과거 인벤토리와의 관계**: 이전 인벤토리 표에 "자원순환과"/"자원회수시설과"로
  제공부서명이 엇갈리고 OA번호도 모순되게 기재되어 있던 항목(`get_chimney_emission_measuremen`)이
  이 데이터셋으로 확인됨. 정확한 제공부서명은 명세서 원문 그대로 "기후환경본부
  자원회수시설추진단 자원회수시설과"로 확정한다.

### 1-6. 서울시 권역별 실시간 대기환경 현황 (RealtimeCityAir) — 신규 추가 아님, 기존 도구 개명 대상

**중요**: 이 데이터셋은 실측 대조 결과 **이미 기존 저장소에 구현되어 있음이
확인되었다.** 다만 `get_realtime_air_quality`라는 잘못된 이름("자치구별"을
의미)으로 구현되어 있었다 — 0-1절 참고. 따라서 이번 작업에서는 신규 구현이
아니라 **개명 및 문서 정정**만 수행한다.

- **제공부서**: 기후환경본부 대기정책과
- **설명**: 대기환경지수·미세먼지·오존·이산화질소·일산화탄소·아황산가스 등의
  권역별(도심권/동북권/동남권/서북권/서남권) 실시간 대기환경정보
- **선택 파라미터**: `SAREA_NM`(권역명: 도심권/동북권/동남권/서북권/서남권), `MSRSTN_NM`(측정소명)
- **응답 필드**: `MSRMT_DT`(측정일시), `SAREA_NM`(권역명), `MSRSTN_NM`(측정소명), `PM`(미세먼지,
  ㎍/㎥), `FPM`(초미세먼지, ㎍/㎥), `OZON`(오존, ppm), `NTDX`(이산화질소, ppm), `CBMX`(일산화탄소, ppm),
  `SPDX`(아황산가스, ppm), `CAI_GRD`(통합대기환경등급, 예: "나쁨"/"보통"), `CAI_IDX`(통합대기환경지수),
  `CRST_SBSTN`(지수결정물질)
- **필요 작업**: `main.py`에서 함수명 `get_realtime_air_quality` → `get_zonal_realtime_air_quality`로
  변경, docstring/설명을 "권역별"로 정정. API 호출 로직(URL, 파싱)은 이미
  정상 동작하므로 변경하지 않는다.
- **기존 별도 프로젝트와의 구분(참고)**: 이미 배포된 `seoul-realtime-air-by-region-mcp`
  (서울시 권역별 실시간 대기환경 MCP)와 이름이 유사하나 서로 다른 MCP
  프로젝트이며, 이번 개명과는 무관하다. 또한 기존 16개 중
  `get_zonal_hourly_air_quality`(OA-2221, 서비스명 TimeAverageCityAir
  계열)와도 "권역별"이라는 표현이 겹치므로, docstring에 "실시간"
  (RealtimeCityAir, 현재 시점 스냅샷) vs "기간별 시간평균"(TimeAverageCityAir,
  과거 특정 시각 조회)의 차이를 명확히 구분해 표기한다.

### 1-7. 서울시 실시간 자치구별 대기환경 현황 (ListAirQualityByDistrictService) — 진짜 신규 추가

- **제공부서**: 기후환경본부 대기정책과
- **설명**: 25개 자치구 전체 또는 원하는 자치구의 실시간 대기환경정보(통합대기환경지수와
  등급, 지수결정물질, PM10, 오존, 이산화질소, 일산화탄소, 아황산가스)
- **선택 파라미터**: `MSRSTN_PBADMS_CD`(측정소 행정코드 — 25개 자치구별 코드, 예:
  111123=종로구)
- **응답 필드**: `MSRMT_YMD`(측정날짜), `MSRSTN_PBADMS_CD`(측정소 행정코드), `MSRSTN_NM`(측정소명),
  `CAI`(통합대기환경지수), `CAI_GRD`(통합대기환경지수 등급), `CRST_SBSTN`(지수결정물질),
  `NTDX`(이산화질소, ppm), `OZON`(오존, ppm), `CBMX`(일산화탄소, ppm), `SPDX`(아황산가스, ppm),
  `PM`(미세먼지, ㎍/㎥), `FPM`(초미세먼지, ㎍/㎥)
- **갱신 주기**: 정기(매일, 1시간 단위)
- **라이선스**: 공공누리 1유형출처표시
- **주의**: 이 데이터셋이 기존에 `RealtimeCityAir`로 잘못 구현되어 있던 것과
  진짜 다른 API이므로, 반드시 새 함수로 구현하고 `main.py` 528행 부근의
  기존 `RealtimeCityAir` 호출부와 혼동하지 않는다.

### 1-8. (참고) 서울시 시간 평균 대기오염도 정보 — 표기 정정만 반영

이번 확장 대상은 아니며, 기존 16개 중 이미 포함된 `get_hourly_air_quality`(OA-2275)의
**화면 표시명 표기를 정정**한다. 인벤토리 표에는 "서울시 시간평균 대기오염도 정보"
(붙여쓰기)로 기재되어 있었으나, 원본 명세서 확인 결과 정확한 명칭은
**"서울시 시간 평균 대기오염도 정보"(띄어쓰기 있음)**이다. README.md Dataset Registry
표 작성 시 이 정정된 표기를 사용한다.

---

## 2. MCP 툴 설계

### 2-1. 개명 대상 (1개, 코드 로직 변경 없음)

| 기존 도구명 | 신규 도구명 | 실제 데이터셋 | SERVICE | 변경 내용 |
|---|---|---|---|---|
| `get_realtime_air_quality` | `get_zonal_realtime_air_quality` | 서울시 권역별 실시간 대기환경 현황 | RealtimeCityAir | 이름·설명·docstring만 정정, API 호출 로직 그대로 유지 |

### 2-2. 신규 추가 도구 (6개)

각 데이터셋이 서로 다른 응답 구조/파라미터를 가지므로, 1API=1툴 형태로 6개 도구를
신규 추가한다(공유 가능한 엔드포인트 구조가 없어 통합 여지가 없음).

| 신규 도구명 | 데이터셋 | SERVICE |
|---|---|---|
| `get_realtime_air_quality` | 서울시 실시간 자치구별 대기환경 현황 | ListAirQualityByDistrictService |
| `get_yearly_ozone_alerts` | 서울시 연도별 오존 경보발령 현황 | YearlyOzoneIssue |
| `get_station_admin_codes` | 서울시 지역 구별 측정소 행정코드 정보 | SearchMeasuringSTNOfAirQualityService |
| `get_yearly_air_quality` | 서울시 년도별 평균 대기오염도 정보 | YearlyAverageAirQuality |
| `get_yearly_pm25_alerts` | 서울시 초미세먼지 연도별 발령정보 | yearMicroDustInfo |
| `get_chimney_emission_info` | 서울시 굴뚝 측정 정보 | CleanSYSService |

**주의**: `get_realtime_air_quality`라는 이름이 개명 전 기존 도구에서 쓰이던
이름과 동일하다. 개명 작업(2-1절)을 먼저 수행해 그 이름을 비운 뒤에, 이
이름을 진짜 자치구별 데이터셋에 새로 부여해야 한다 — 순서가 바뀌면 이름이
충돌한다 (5절 진행 순서 참고).

(기존 16개 중 나머지 15개 도구는 그대로 유지. 1-8절은 문서 표기만 정정이므로
코드 변경 없음)

확장 후 전체 도구 수: 16 + 6(신규) = **22개** (개명 대상 1개는 기존 16개 안에
이미 포함되어 있으므로 개수에 영향 없음)

※ v5 지침에 따라 "OO 외 N종" 축약 없이, 아래 3절 및 README.md에 신규 6개 데이터셋
전체 명칭을 온전히 나열한다.

---

## 3. 신규 추가되는 6개 데이터셋 전체 명칭 (축약 없이 전체 나열)

1. 서울시 실시간 자치구별 대기환경 현황
2. 서울시 연도별 오존 경보발령 현황
3. 서울시 지역 구별 측정소 행정코드 정보
4. 서울시 년도별 평균 대기오염도 정보
5. 서울시 초미세먼지 연도별 발령정보
6. 서울시 굴뚝 측정 정보

※ "서울시 권역별 실시간 대기환경 현황"은 신규 추가가 아니라 기존 도구
개명 대상이므로 이 목록에서 제외한다 (0-1, 2-1절 참고).

---

## 4. 기술 스택 및 디렉토리 구조

- 기존 `seoul-air-quality-mcp` 저장소에 통합(신규 프로젝트 아님)
- FastMCP, Python, `stateless_http=True` 유지
- 기존 `server.py`(또는 `main.py`)에 신규 6개 함수 추가
- 기존 `seoul_api.py`(또는 `data_api.py`)의 공통 호출 함수 재사용 (JSON 우선,
  XML 폴백 로직 그대로 적용)
- `_DATASET_INFO` 딕셔너리(있는 경우)에 신규 6개 메타데이터(서비스명, 제공부서,
  원본 URL) 추가

---

## 5. 진행 순서

**순서가 중요하다 — 특히 1~2번은 반드시 이 순서로 진행할 것 (이름 충돌 방지).**

1. 기존 저장소 최신 상태 pull
2. **개명 작업 먼저 수행**: `main.py`에서 기존 `get_realtime_air_quality` 함수를
   `get_zonal_realtime_air_quality`로 이름 변경, docstring/설명을 "권역별"에
   맞게 정정 (2-1절). API 호출 로직(URL, 파싱 코드)은 절대 건드리지 않는다.
3. **개명 완료 후 신규 6개 함수 구현** (2-2절, 기존 파일 구조 그대로 사용,
   신규 파일 생성 최소화). 이때 `get_realtime_air_quality`라는 이름을 진짜
   자치구별 데이터셋(`ListAirQualityByDistrictService`)에 새로 부여한다 —
   2번에서 개명이 끝나 있어야 이름이 비어 있어 충돌하지 않는다.
4. 각 함수 docstring에 필드/단위 명시 + 출처 표기 필수 문구 삽입
5. 로컬 실측 테스트:
   - **개명된 `get_zonal_realtime_air_quality`가 여전히 정상 동작하는지
     회귀 테스트** — 이름만 바뀌었을 뿐 로직은 그대로이므로 기존과 동일한
     응답이 나와야 한다.
   - **신규 `get_realtime_air_quality`(자치구별)가 `ListAirQualityByDistrictService`를
     정확히 호출하는지, 응답 필드가 1-7절 명세와 일치하는지 확인**
   - 1-1, 1-4의 빈 값(`MAX_DNST` 등) 처리 확인 — 발령 0회 연도에서 안전하게
     처리되는지
   - 1-3의 `MSRMT_YR` 필수 파라미터 누락 시 ERROR-300 재현 확인
   - 1-5의 `MSRMT_VL`이 "운휴중" 등 비숫자 문자열로 오는 케이스 확인 — 다른
     상태 문자열 존재 여부도 함께 확인
   - 기존 `get_zonal_hourly_air_quality`, 그리고 별도 프로젝트인
     `seoul-realtime-air-by-region-mcp`와 이번 개명 대상의 최종 중복 여부 재확인
6. README.md/DEVPLAN.md/CLAUDE.md 갱신 (Dataset Registry 표를 16개→22개로 확장,
   개명 사실과 1-8절 표기 정정을 모두 반영. 특히 "배포 검증 이력"에 이번
   버그 발견·수정 사실을 명확히 기록 — 향후 같은 혼동 재발 방지 목적)
7. DEVLOG.md에 이번 확장 작업(버그 수정 포함) 기록 추가
8. git add/commit/push
9. **여기서 정지** — 배포(`flyctl deploy`)는 사용자가 PowerShell에서 직접 수행

---

## 6. 사용자가 먼저 할 일

- 기존 `seoul-air-quality-mcp` 프로젝트 폴더(`C:\Users\hwang\Projects\seoul-air-quality-mcp`)로
  이동해 최신 상태인지 확인
- 이 DEVPLAN.md를 포함한 문서 4종을 기존 프로젝트에 반영할 때는, 신규 프로젝트
  부트스트랩(`run-new-mcp-project.bat`)이 아니라 **기존 프로젝트에서 직접 Claude
  Code를 실행**하는 방식으로 진행 (신규 저장소 생성 아님)
- API 키는 기존 `.env`의 `SEOUL_API_KEY`를 그대로 사용 (신규 발급 불필요)

---

## 7. 저장소 설명(Description 제안)

> 서울시 실시간·기간별·연도별 대기환경 정보, 미세먼지·오존·초미세먼지 경보발령 현황, 측정소·시설 정보 등 22개 도구를 제공하는 서울시 대기환경정보 MCP 서버
